---
meta:
  environment: NAME
  dns_root: microbosh
  security_groups:
    - port_forwarding
  stemcell:
    name: STEMCELL

director_uuid: PLACEHOLDER-DIRECTOR-UUID

releases:
  - name: port-forwarding
    version: latest
